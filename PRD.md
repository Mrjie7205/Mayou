# Mayou PRD

> 转转麻将云游戏辅助工具 · 产品需求文档
> 最后更新：2026-05-12

## 1. 项目定位

Mayou 是一个**个人使用、低优先级**的桌面工具，每周投入约 2 小时。

它在 Max 的 Windows 工作站（Y9000K）上运行，监听一个云游戏窗口里的转转麻将牌桌，实时识别局面，给出记牌信息和出牌建议。Max **手动**按建议出牌。

它不是产品，没有用户增长目标，没有"做好做大"的预期。**优先级低于 trading-terminal 和 MarketAgent**，定位是"换脑子"的技术练手项目。

## 2. 项目边界

### 2.1 在范围内

- 单一云游戏平台（待选定，见 ADR-001）
- 转转麻将单一玩法
- 实时屏幕识别、状态追踪、建议显示
- 自己手动出牌

### 2.2 不在范围内

- **不支持其他麻将变种**（株洲碰胡、跑得快、四川麻将等，第一阶段全部不做）
- **不支持手机端**（云游戏在 PC 上跑）
- **不做自动出牌**（仅显示建议）
- **不做多平台适配**（每换平台 CV 层重新校准，单独 PRD 走起）
- **不接神经网络 AI**（性价比不够，走经典 AI 路线）
- **不做反检测对抗**（行为侧由 Max 自己控制使用强度）

### 2.3 合规边界（硬约束）

- 仅用于**欢乐场**（无现金兑换的娱乐场）
- 永不分发、永不出售、永不开源
- 永不用于含现金/积分兑换的房间
- 账号封禁风险由 Max 自担

## 3. 用户路径

```
启动 Mayou
    ↓
启动云游戏，进入麻将桌
    ↓
（首次）框选屏幕区域 → 保存为该平台配置
（之后）自动加载配置
    ↓
程序开始监听，UI 显示实时建议
    ↓
Max 按建议手动出牌
```

## 4. 功能里程碑

### MVP-1：记牌器（约 20 周）

**交付物**：能稳定追踪一整局牌局的工具，但不给出牌建议。

功能：
- 自己手牌识别（14 张）
- 四家弃牌区识别
- 四家碰杠区识别
- 状态机事件追踪（摸/打/碰/杠/胡/流局）
- UI 显示：
  - 自己手牌
  - 34 种牌的剩余数量
  - 最近事件日志
  - 简单危险牌提示（仅基于现物）

**验收标准**：
- 一局牌打完，状态机记录的事件序列与实际一致（≥95% 准确）
- 单张牌识别准确率 ≥99%
- 端到端延迟 ≤500ms

### MVP-2：完整辅助（约 38 周累计）

**交付物**：MVP-1 + 出牌建议。

新增功能：
- shanten 计算（转转麻将规则适配）
- 蒙特卡洛进攻评分（每张可打牌的胡牌概率 + 期望番数）
- 防守评分（现物 / 筋 / 壁 / 读牌）
- 进攻 vs 防守融合打分
- 推荐出牌高亮显示，悬停看分项

**验收标准**：
- 建议给出延迟 ≤2 秒
- 在欢乐场对线，Max 主观感觉"中等水平人类玩家"

### MVP-3+（未规划）

视 MVP-2 用下来的痛点而定。可能方向：番型识别优化、对手风格建模、回放复盘。**不预先承诺**。

## 5. 技术选型概览

详见 [docs/01-architecture.md](docs/01-architecture.md)。

| 层 | 选型 | 主要理由 |
|---|---|---|
| 截图 | mss + pygetwindow | 简单、快、零依赖坑 |
| 识别 | OpenCV 模板匹配 | 单 UI 固定，模板法准确率够 |
| 状态机 | 纯 Python 类 | 不复杂，不需要框架 |
| 引擎 | python-mahjong fork + 自写蒙特卡洛 | 复用骨架，改胡牌型 |
| UI | PySide6 | 悬浮置顶体验最好 |
| 整体 | Python 单进程 | 简单胜过一切 |

## 6. 工程约束

继承自 trading-terminal：

1. **Max 不直接编辑文件**。所有数据通过 UI 或 Claude Code 修改。
2. **前端优先**。新功能涉及写入的，必须先有前端入口才能开发后端。
3. **配置版本化**。区域坐标、模板路径、引擎权重等配置都纳入 Git。
4. **永远可调试**。每层都要能单独可视化（CV 看识别框、状态机看事件流、引擎看分项打分）。

## 7. 风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| 云游戏平台改版 | CV 层失效 | 接受现实，重新框选 + 补模板。改版频率预计 <2 次/年 |
| 前期模板库不足 | 识别率低，体验差 | 接受 3-4 周低识别率期，专门做模板采集工具加速积累 |
| 状态机边角情况多 | 牌局事件漏检 | 日志区可视化，发现一个补一个，不追求一次到位 |
| 引擎建议质量不稳 | 出牌建议不可信 | 进攻分/防守分分项展示，Max 自己判断采纳 |
| 行为模式被风控 | 账号封禁 | 不每手都跟建议、保留人工决策比例 |
| Max 投入时间不稳 | 项目长期烂尾 | 接受。这是练手项目，烂尾也无损 |

## 8. 成功的样子

**不是**：成为麻将外挂、稳定盈利、技术博客爆款。

**是**：
- 半年后有一个能用的记牌器，帮 Max 在欢乐场打得更舒服
- 整套"截屏→识别→状态→决策→显示"的技术栈成为 Max 工具箱里的一部分，未来某个 AI 实施咨询场景能迁移复用
- 过程本身让 Max 在工作之外有个能动脑的小项目
