# Mayou dependencies
# Requires Python >= 3.11
# (Comments kept ASCII-only to avoid pip GBK decode errors on Chinese Windows)

# Screen capture / window
mss>=9.0.1
pygetwindow>=0.0.9

# Image processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# UI framework
PySide6>=6.5.0

# Zihai (zhuzhou penghu) engine is hand-written, see ADR-002 amendment.
# No third-party engine dependency.

# Test tooling
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional dev tools (uncomment if you want them):
# black>=23.7.0      # formatter
# ruff>=0.0.290      # linter
# mypy>=1.5.0        # type checker
