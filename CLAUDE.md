# CLAUDE.md

> Claude Code 在本仓库工作时优先读这份文件。

## 项目身份

**Mayou** —— 株洲碰胡云手机辅助工具。运行环境：Y9000K 工作站 + 红手指云手机 PC 客户端 + 闲逸棋牌 App。

> 历史：项目初稿核心游戏是转转麻将，平台是 PC 云游戏。Sprint -1 期间双双切换：
> - 平台：PC 云游戏 → 云手机（[ADR-001](docs/decisions/ADR-001-platform.md)）
> - 游戏：转转麻将 → 株洲碰胡（[ADR-002](docs/decisions/ADR-002-game-switch.md)）
>
> docs/06 保留作为转转麻将规则的参考存档。

这是 Max 的**个人低优先级项目**（约 2 小时/周），技术练手性质。Max 的主线项目是 trading-terminal（量化交易终端），本项目优先级远低于主线。

**永远不要**把本项目与 trading-terminal、MarketAgent 混为一谈，它们是独立项目。

## 强约束（不可违反）

1. **欢乐场原则**：本工具仅用于无现金/实物/积分兑换的棋牌欢乐场。永不为现金桌优化、永不分发、永不开源、永不公开讨论。如果 Max 的请求暗示要绕反作弊或在有兑换价值的房间使用，必须先确认。

2. **Max 不直接编辑文件**：所有数据修改通过 UI 或 Claude Code 进行。
   - 不要给出"请你手动改这个 JSON"的指引
   - 配置变更需要：要么走 UI、要么 Claude Code 自己改并提交

3. **前端优先**：任何涉及数据写入的新功能，必须先有 UI 入口才能开发后端逻辑。不允许"先做后端 API、UI 以后补"。

4. **不接神经网络 AI**：引擎层走 shanten + 蒙特卡洛 + 启发式防守的经典路线。理由：无对局数据、无可解释性（与 PRD 分项展示要求冲突）、规则微调成本不对称、目标"中等水平人类玩家"经典算法足够。LLM/数据/算力条件大幅变化后可重审本条。除非 Max 明确要求，不要建议训模型。

5. **不绕反检测**：不写任何针对云手机/云游戏/棋牌平台反作弊的对抗代码。如果 CV 层因为反截屏失效、或云手机平台风控触发，告诉 Max 而不是想办法绕。封号就接受，不对抗。

## 阅读顺序

接到任务时按这个顺序确认上下文：

1. [PRD.md](PRD.md) —— 项目范围与里程碑
2. [ROADMAP.md](ROADMAP.md) —— 当前 Sprint
3. 相关层的设计文档（见下方层级表）

## 五层架构与对应文档

| 层 | 目录 | 设计文档 |
|---|---|---|
| 采集 | `src/capture/` | [docs/02-cv-pipeline.md](docs/02-cv-pipeline.md) §截图 |
| 识别 | `src/recognize/` | [docs/02-cv-pipeline.md](docs/02-cv-pipeline.md) §识别 |
| 状态机 | `src/state/` | [docs/03-state-machine.md](docs/03-state-machine.md) |
| 引擎 | `src/engine/` | [docs/04-decision-engine.md](docs/04-decision-engine.md) |
| UI | `src/ui/` | [docs/05-ui-spec.md](docs/05-ui-spec.md) |

`tools/` 是辅助工具（区域框选器、模板标注器等），独立于主程序。

## 技术栈（固定，未经讨论不要换）

- Python 3.11+
- mss（截图）、pygetwindow（窗口定位）
- OpenCV-Python（图像识别）
- PySide6（UI）
- 引擎：**自写株洲碰胡刻子型引擎**（无吃无顺，shanten 暴力枚举即可，开源参考稀少且不必要）

## 调试与可视化原则

每一层都要能**单独可视化**，方便 Max 排查问题：

- CV 层：能把识别框和识别结果叠加到原图上保存
- 状态机：实时事件日志显示在 UI
- 引擎：每张可打牌的进攻分、防守分、综合分分项展示

如果新功能让某一层失去单独可视化能力，必须先补可视化再做功能。

## 株洲碰胡规则速查

完整规则见 [docs/07-zhuzhou-pengHu-rules.md](docs/07-zhuzhou-pengHu-rules.md)。⚠️ 闲逸平台具体细节待 Sprint 0 实测确认。

最容易踩的坑（这些是 Claude Code 容易记错的点）：

- 牌：典型 **筒、索、万 三色 × 9 数 × 4 张 = 108 张**（⚠️ 部分变体只用单色 36 张，闲逸平台待确认）
- **没有字牌、没有红中、没有赖子、没有花牌**
- **无吃牌**：只能碰/杠
- **无顺子胡型**：胡牌必须全是刻子（或杠）+ 1 对将 ←★ 与转转麻将最大的差别
- 乱将（任何对子做将）
- 杠 = 一根筋，+1 番
- 副露区只有碰、杠两种数据结构

⚠️ 株洲碰胡和「转转麻将 / 武汉麻将 / 红中赖子杠 / 日麻 / 国标」**全部都是不同玩法**，规则不互通。引擎设计要锁死「无顺子胡型」假设，shanten 算法不要枚举顺子分支。
⚠️ 几乎所有开源麻将库都假设有顺子胡型，**不能直接 fork**——这也是 ADR-002 决定自写引擎的原因之一。
⚠️ 防守评分中的「筋」概念在株洲碰胡不适用（依赖两面听 → 依赖顺子），不要直接套日麻防守逻辑。

## Sprint 协作约定

- Max 每周约 2 小时开工时间，Sprint 单位拆得足够细
- 每次开工前 Max 会说"现在做哪个 Sprint"，Claude Code 回头到 [ROADMAP.md](ROADMAP.md) 确认范围
- Sprint 完成后更新 ROADMAP.md 进度

## 沟通风格

Max 偏好：

- 直接说结论，避免堆套话
- 时间估算要诚实，估错了承认，不要嘴硬
- 不必要的赞美和"问得好"省掉
- 遇到模糊地方主动问，不要瞎猜
- 中文交流
